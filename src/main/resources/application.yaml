server:
  port: 8080
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/test
    username: root
    password:
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
  mail:
    host: localhost
    port: 1025
    username: ''
    password: ''
app:
  security:
    api-key: 123456-secret-key
  tokenization:
    reject-probability: 0.10
  payment:
    approval-probability: 0.75
    retry-count: 3
  product:
    min-stock-display: 1

